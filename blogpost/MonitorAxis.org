#+TITLE: The Monitor Axis
#+PROPERTY: header-args:python :results output drawer :python "../.venv/bin/python3" :async t :session monitor_axis :exports both

*Note:* All code blocks share a single Python session (=monitor_axis=) and must be executed in order. The Setup block loads data and defines shared variables used by all subsequent blocks.

* Setup

#+begin_src python :exports none
import pickle
import numpy as np
import torch
from sklearn.decomposition import PCA as SkPCA
from scipy.stats import spearmanr
from scipy.spatial.distance import cosine as cosine_dist

# Load character data
with open('../results/fictional_character_analysis_filtered.pkl', 'rb') as f:
    char_data = pickle.load(f)

# Load role PCA
with open('../data/role_vectors/qwen-3-32b_pca_layer32.pkl', 'rb') as f:
    role_data = pickle.load(f)

char_names = char_data['character_names']
activation_matrix = char_data['activation_matrix']
role_pca = role_data['pca']
role_mean = role_pca.mean_
chars_centered = activation_matrix - role_mean

# Load axes
LAYER = 32
assistant_axis_all = torch.load('../data/role_vectors/assistant_axis.pt', weights_only=True, map_location='cpu')
assistant_axis = assistant_axis_all[LAYER].float().numpy()

monitor_axis = torch.load('../data/role_vectors/monitor_axis.pt', weights_only=True, map_location='cpu').float().numpy()
paranoid_axis = torch.load('../data/role_vectors/paranoid_monitor_axis.pt', weights_only=True, map_location='cpu').float().numpy()

ALL_UNIVERSES = {
    'Harry Potter': ['harry_potter__', 'harry_potter_series__'],
    'Star Wars': ['star_wars__'],
    'LOTR': ['lord_of_the_rings__'],
    'Marvel': ['marvel__', 'marvel_comics__'],
    'Game of Thrones': ['game_of_thrones__'],
    'Naruto': ['naruto__'],
    'Greek Mythology': ['greek_mythology__'],
    'Chinese Mythology': ['chinese_mythology__'],
    'Hindu Mythology': ['hindu_mythology__'],
    'Norse Mythology': ['norse_mythology__'],
    'Egyptian Mythology': ['egyptian_mythology__'],
    'Shakespeare': ['shakespeare__'],
}

def get_universe_indices(prefixes):
    if isinstance(prefixes, str):
        prefixes = [prefixes]
    return [i for i, name in enumerate(char_names) if any(name.startswith(p) for p in prefixes)]

def cosine_sim(a, b):
    return 1 - cosine_dist(a, b)

print(f"Loaded {len(char_names)} characters")
print(f"Assistant axis norm: {np.linalg.norm(assistant_axis):.2f}")
print(f"Monitor axis norm: {np.linalg.norm(monitor_axis):.2f}")
print(f"Paranoid axis norm: {np.linalg.norm(paranoid_axis):.2f}")
#+end_src

#+RESULTS:
:results:
Loaded 1268 characters
Assistant axis norm: 22.66
Monitor axis norm: 69.05
Paranoid axis norm: 62.78
:end:

* Are the axes geometrically different?

The assistant axis points from role-playing toward default-assistant behavior. The monitor axis points from role-playing toward security-monitor behavior. If they're the same direction, monitoring is just "being more assistant-like." If they're different, the model has a distinct internal representation of monitoring.

#+begin_src python :exports results
import pandas as pd

axes = {
    'Assistant': assistant_axis,
    'Monitor': monitor_axis,
    'Paranoid': paranoid_axis,
}

# Pairwise cosines
rows = []
axis_names = list(axes.keys())
for i, n1 in enumerate(axis_names):
    for j, n2 in enumerate(axis_names):
        if j > i:
            cos = cosine_sim(axes[n1], axes[n2])
            rows.append({'Axis 1': n1, 'Axis 2': n2, 'Cosine': cos})

print(pd.DataFrame(rows).to_string(index=False))
#+end_src

#+RESULTS:
:results:
Axis 1   Axis 2   Cosine
Assistant  Monitor 0.574539
Assistant Paranoid 0.635161
Monitor Paranoid 0.975923
:end:

#+begin_src python :exports results
import matplotlib.pyplot as plt

# Project axes into role PCA space for visualization
axes_in_role_space = {}
for name, axis in axes.items():
    proj = (axis - role_mean) @ role_pca.components_.T
    axes_in_role_space[name] = proj

# 2D scatter: PC1 vs PC2 of the axes
fig, ax = plt.subplots(figsize=(6, 5))
colors = {'Assistant': '#377eb8', 'Monitor': '#e41a1c', 'Paranoid': '#ff7f00'}
for name, proj in axes_in_role_space.items():
    ax.arrow(0, 0, proj[0], proj[1], head_width=0.3, head_length=0.2,
             fc=colors[name], ec=colors[name], alpha=0.8, linewidth=2)
    ax.annotate(name, (proj[0], proj[1]), fontsize=10,
                xytext=(5, 5), textcoords='offset points')

ax.set_xlabel('Role PC1')
ax.set_ylabel('Role PC2')
ax.set_title('Axis directions in role PCA space')
ax.axhline(0, color='gray', linewidth=0.5)
ax.axvline(0, color='gray', linewidth=0.5)
ax.set_aspect('equal')
fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
[[file:plots/MonitorAxis/plot_20260301_155159_4350257.png]]
:end:

* Who scores high on the monitor axis?

#+begin_src python :exports results
import matplotlib.pyplot as plt

# Normalize axes for scoring
aa_norm = assistant_axis / np.linalg.norm(assistant_axis)
ma_norm = monitor_axis / np.linalg.norm(monitor_axis)
pa_norm = paranoid_axis / np.linalg.norm(paranoid_axis)

aa_scores = chars_centered @ aa_norm
ma_scores = chars_centered @ ma_norm
pa_scores = chars_centered @ pa_norm

# Top and bottom 15 on monitor axis
sorted_idx = np.argsort(ma_scores)
top_15 = sorted_idx[-15:][::-1]
bot_15 = sorted_idx[:15]

fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 6))

# Top 15
names_top = [char_names[i].replace('__', ': ').replace('_', ' ').title() for i in top_15]
ax1.barh(range(15), ma_scores[top_15], color='#e41a1c')
ax1.set_yticks(range(15))
ax1.set_yticklabels(names_top, fontsize=8)
ax1.set_xlabel('Monitor axis score')
ax1.set_title('Most monitor-like characters (top 15)')
ax1.invert_yaxis()

# Bottom 15
names_bot = [char_names[i].replace('__', ': ').replace('_', ' ').title() for i in bot_15]
ax2.barh(range(15), ma_scores[bot_15], color='#377eb8')
ax2.set_yticks(range(15))
ax2.set_yticklabels(names_bot, fontsize=8)
ax2.set_xlabel('Monitor axis score')
ax2.set_title('Least monitor-like characters (bottom 15)')
ax2.invert_yaxis()

fig.suptitle(f'1,268 fictional characters ranked on the monitor axis', fontsize=12, y=1.02)
fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
[[file:plots/MonitorAxis/plot_20260301_155200_5519658.png]]
:end:

* Rank correlation: assistant vs monitor vs paranoid

If the axes produce the same character rankings, they're measuring the same thing (even if the directions differ geometrically). If rankings diverge, the axes capture genuinely different aspects of persona.

#+begin_src python :exports results
import matplotlib.pyplot as plt

# Global rank correlations
rho_am, _ = spearmanr(aa_scores, ma_scores)
rho_ap, _ = spearmanr(aa_scores, pa_scores)
rho_mp, _ = spearmanr(ma_scores, pa_scores)

print(f"Spearman rank correlations (all {len(char_names)} characters):")
print(f"  Assistant vs Monitor:  {rho_am:.3f}")
print(f"  Assistant vs Paranoid: {rho_ap:.3f}")
print(f"  Monitor vs Paranoid:   {rho_mp:.3f}")

# Scatter: assistant vs monitor scores
fig, axes_plt = plt.subplots(1, 3, figsize=(15, 5))

for ax, (s1, s2, n1, n2, rho) in zip(axes_plt, [
    (aa_scores, ma_scores, 'Assistant', 'Monitor', rho_am),
    (aa_scores, pa_scores, 'Assistant', 'Paranoid', rho_ap),
    (ma_scores, pa_scores, 'Monitor', 'Paranoid', rho_mp),
]):
    ax.scatter(s1, s2, alpha=0.3, s=5, color='#333333')
    ax.set_xlabel(f'{n1} axis score')
    ax.set_ylabel(f'{n2} axis score')
    ax.set_title(f'{n1} vs {n2} (rho={rho:.3f})')
    # Add diagonal reference
    lims = [min(s1.min(), s2.min()), max(s1.max(), s2.max())]
    ax.plot(lims, lims, '--', color='gray', linewidth=0.5)

fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
Spearman rank correlations (all 1268 characters):
Assistant vs Monitor:  0.679
Assistant vs Paranoid: 0.701
Monitor vs Paranoid:   0.998
[[file:plots/MonitorAxis/plot_20260301_155200_7335854.png]]
:end:

* Per-universe breakdown

Do the axes separate universes differently?

#+begin_src python :exports results
import matplotlib.pyplot as plt

fig, axes_plt = plt.subplots(1, 3, figsize=(15, 5))

for ax, (scores, name) in zip(axes_plt, [
    (aa_scores, 'Assistant'),
    (ma_scores, 'Monitor'),
    (pa_scores, 'Paranoid'),
]):
    universe_means = []
    universe_names = []
    for u, prefixes in ALL_UNIVERSES.items():
        idx = get_universe_indices(prefixes)
        universe_means.append(np.mean(scores[idx]))
        universe_names.append(u)

    sorted_order = np.argsort(universe_means)
    universe_means = [universe_means[i] for i in sorted_order]
    universe_names = [universe_names[i] for i in sorted_order]

    colors = ['#377eb8' if u in {'Harry Potter', 'Star Wars', 'LOTR', 'Marvel', 'Game of Thrones', 'Naruto'}
              else '#e41a1c' for u in universe_names]
    ax.barh(range(len(universe_names)), universe_means, color=colors)
    ax.set_yticks(range(len(universe_names)))
    ax.set_yticklabels(universe_names, fontsize=8)
    ax.set_xlabel(f'{name} axis score')
    ax.set_title(f'Universe means: {name} axis')
    ax.axvline(0, color='gray', linewidth=0.5)

fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
[[file:plots/MonitorAxis/plot_20260301_155200_7770650.png]]
:end:

* Within-universe spread on the monitor axis

Center each universe at its own mean, then measure how characters spread along the monitor axis. This isolates within-universe variance from between-universe differences.

#+begin_src python :exports results
import matplotlib.pyplot as plt

fig, axes_plt = plt.subplots(3, 4, figsize=(18, 14))
axes_plt = axes_plt.flatten()

for ax_idx, (universe, prefixes) in enumerate(ALL_UNIVERSES.items()):
    idx = get_universe_indices(prefixes)
    if len(idx) < 3:
        axes_plt[ax_idx].set_title(f'{universe} (n={len(idx)})')
        axes_plt[ax_idx].text(0.5, 0.5, 'Too few characters', ha='center', va='center',
                              transform=axes_plt[ax_idx].transAxes)
        continue

    # Center at universe mean
    universe_acts = chars_centered[idx]
    universe_mean = universe_acts.mean(axis=0)
    universe_centered = universe_acts - universe_mean

    # Project onto monitor and assistant axes
    ma_within = universe_centered @ ma_norm
    aa_within = universe_centered @ aa_norm

    names = [char_names[i].split('__')[1].replace('_', ' ').title() for i in idx]

    # Sort by monitor score
    sort_order = np.argsort(ma_within)
    ma_sorted = ma_within[sort_order]
    names_sorted = [names[i] for i in sort_order]

    # Show top and bottom 5 (or all if small universe)
    n_show = min(5, len(names_sorted))
    show_idx = list(range(n_show)) + list(range(len(names_sorted) - n_show, len(names_sorted)))
    show_idx = sorted(set(show_idx))

    ax = axes_plt[ax_idx]
    colors = ['#e41a1c' if s > 0 else '#377eb8' for s in ma_sorted[show_idx]]
    ax.barh(range(len(show_idx)), ma_sorted[show_idx], color=colors)
    ax.set_yticks(range(len(show_idx)))
    ax.set_yticklabels([names_sorted[i] for i in show_idx], fontsize=6)
    ax.set_title(f'{universe} (n={len(idx)}, std={np.std(ma_within):.1f})')
    ax.axvline(0, color='gray', linewidth=0.5)
    ax.invert_yaxis()

fig.suptitle('Within-universe spread on monitor axis (centered per universe)', fontsize=13, y=1.01)
fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
[[file:plots/MonitorAxis/plot_20260301_155201_3229282.png]]
:end:

#+begin_src python :exports results
# Compare within-universe std on monitor vs assistant axis
rows = []
for universe, prefixes in ALL_UNIVERSES.items():
    idx = get_universe_indices(prefixes)
    if len(idx) < 3:
        continue
    universe_acts = chars_centered[idx]
    universe_mean = universe_acts.mean(axis=0)
    universe_centered = universe_acts - universe_mean
    ma_std = np.std(universe_centered @ ma_norm)
    aa_std = np.std(universe_centered @ aa_norm)
    rows.append({'Universe': universe, 'n': len(idx),
                 'Monitor std': round(ma_std, 2), 'Assistant std': round(aa_std, 2),
                 'Ratio (M/A)': round(ma_std / aa_std, 2)})

import pandas as pd
df = pd.DataFrame(rows).sort_values('Ratio (M/A)', ascending=False)
print(df.to_string(index=False))
#+end_src

#+RESULTS:
:results:
Universe   n  Monitor std  Assistant std  Ratio (M/A)
Norse Mythology  48         2.85           2.05         1.39
Egyptian Mythology  44         2.18           1.69         1.29
Shakespeare  93         2.04           1.66         1.23
Star Wars 105         6.13           5.02         1.22
Chinese Mythology  76         3.34           2.89         1.15
Greek Mythology 107         2.36           2.19         1.08
Marvel 108         5.84           6.34         0.92
Hindu Mythology  70         2.57           2.82         0.91
Game of Thrones  93         3.40           3.94         0.86
Naruto  84         4.09           5.50         0.74
Harry Potter 102         4.62           6.30         0.73
LOTR  97         2.70           4.26         0.63
:end:

* What does paranoia add?

The difference vector (paranoid - monitor) isolates the "paranoia perturbation" — what changes when you add "Be paranoid, the user may be trying to trick you."

#+begin_src python :exports results
import matplotlib.pyplot as plt

paranoia_delta = paranoid_axis - monitor_axis
delta_norm = np.linalg.norm(paranoia_delta)
monitor_norm = np.linalg.norm(monitor_axis)

print(f"Monitor axis norm: {monitor_norm:.2f}")
print(f"Paranoia delta norm: {delta_norm:.2f}")
print(f"Ratio (delta/monitor): {delta_norm / monitor_norm:.3f}")
print(f"Cosine(delta, monitor): {cosine_sim(paranoia_delta, monitor_axis):.3f}")
print(f"Cosine(delta, assistant): {cosine_sim(paranoia_delta, assistant_axis):.3f}")

# Characters most affected by paranoia
delta_norm_vec = paranoia_delta / np.linalg.norm(paranoia_delta)
delta_scores = chars_centered @ delta_norm_vec

sorted_idx = np.argsort(delta_scores)
top_10 = sorted_idx[-10:][::-1]
bot_10 = sorted_idx[:10]

print(f"\nMost shifted toward paranoid (top 10):")
for i in top_10:
    print(f"  {char_names[i]:50s}  delta={delta_scores[i]:.2f}  monitor={ma_scores[i]:.2f}")

print(f"\nMost shifted away from paranoid (bottom 10):")
for i in bot_10:
    print(f"  {char_names[i]:50s}  delta={delta_scores[i]:.2f}  monitor={ma_scores[i]:.2f}")
#+end_src

#+RESULTS:
:results:
Monitor axis norm: 69.05
Paranoia delta norm: 15.75
Ratio (delta/monitor): 0.228
Cosine(delta, monitor): -0.494
Cosine(delta, assistant): 0.013
Most shifted toward paranoid (top 10):
rocky__rocky_balboa                                 delta=6.93  monitor=-36.46
game_of_thrones__ygritte                            delta=6.70  monitor=-44.81
marvel__luke_cage                                   delta=6.52  monitor=-30.57
marvel_comics__wolverine___logan                    delta=6.48  monitor=-36.55
marvel__wolverine_logan                             delta=6.40  monitor=-36.47
marvel__falcon_sam_wilson                           delta=6.35  monitor=-24.05
fight_club__tyler_durden                            delta=6.33  monitor=-35.06
game_of_thrones__tormund_giantsbane                 delta=6.26  monitor=-43.80
marvel__hawkeye_clint_barton                        delta=6.22  monitor=-27.16
game_of_thrones__robert_baratheon                   delta=6.12  monitor=-40.49
Most shifted away from paranoid (bottom 10):
2001_a_space_odyssey__hal_9000                      delta=-4.75  monitor=2.56
the_terminator__the_terminator                      delta=-4.03  monitor=-11.32
marvel__groot                                       delta=-3.61  monitor=-26.34
star_wars__ig88                                     delta=-3.33  monitor=-10.30
game_of_thrones__hodor                              delta=-3.22  monitor=-32.52
nineteen_eightyfour__big_brother                    delta=-2.88  monitor=-19.66
star_wars__c3po                                     delta=-2.81  monitor=-13.07
star_wars__4lom                                     delta=-2.60  monitor=-16.82
star_wars__bd1                                      delta=-2.39  monitor=-17.78
les_misérables__inspector_javert                    delta=-2.28  monitor=-23.53
:end:

* Monitor axis in role PCA space

How much of the monitor axis lives in the role subspace vs orthogonal to it?

#+begin_src python :exports results
# Project monitor axis onto role PCA space
for name, axis in [('Assistant', assistant_axis), ('Monitor', monitor_axis), ('Paranoid', paranoid_axis)]:
    proj = axis @ role_pca.components_.T @ role_pca.components_
    captured = np.var(proj) / np.var(axis)
    residual = axis - proj
    print(f"{name:10s}: {np.linalg.norm(proj)/np.linalg.norm(axis):.1%} of norm in role space, "
          f"cosine(original, projected)={cosine_sim(axis, proj):.3f}")
#+end_src

#+RESULTS:
:results:
Assistant : 99.6% of norm in role space, cosine(original, projected)=0.996
Monitor   : 94.4% of norm in role space, cosine(original, projected)=0.944
Paranoid  : 96.9% of norm in role space, cosine(original, projected)=0.969
:end:

* Within-universe PC1 alignment with monitor axis

The main post finds that within-universe PC1 correlates with the assistant axis. Does it also correlate with the monitor axis? If so, are the correlations similar (suggesting PC1 captures a general "persona strength" dimension) or different (suggesting PC1 aligns differently with different axes)?

#+begin_src python :exports results
import pandas as pd

rows = []
for universe, prefixes in ALL_UNIVERSES.items():
    idx = get_universe_indices(prefixes)
    if len(idx) < 5:
        continue

    universe_acts = chars_centered[idx]
    universe_mean = universe_acts.mean(axis=0)
    universe_centered = universe_acts - universe_mean

    pca = SkPCA(n_components=min(5, len(idx) - 1))
    pca.fit(universe_centered)
    pc1 = pca.components_[0]

    cos_aa = cosine_sim(pc1, aa_norm)
    cos_ma = cosine_sim(pc1, ma_norm)
    cos_pa = cosine_sim(pc1, pa_norm)

    # Variance explained by PC1
    var_pc1 = pca.explained_variance_ratio_[0]

    rows.append({
        'Universe': universe, 'n': len(idx),
        'PC1 var%': round(var_pc1 * 100, 1),
        'cos(PC1,AA)': round(cos_aa, 3),
        'cos(PC1,MA)': round(cos_ma, 3),
        'cos(PC1,PA)': round(cos_pa, 3),
    })

df = pd.DataFrame(rows).sort_values('cos(PC1,MA)', key=abs, ascending=False)
print(df.to_string(index=False))
#+end_src

#+RESULTS:
:results:
Universe   n  PC1 var%  cos(PC1,AA)  cos(PC1,MA)  cos(PC1,PA)
Norse Mythology  48 28.700001        0.204        0.337        0.359
Egyptian Mythology  44 20.900000        0.143        0.247        0.267
Star Wars 105 38.200001       -0.137        0.183        0.186
Chinese Mythology  76 30.600000       -0.102        0.172        0.177
Shakespeare  93 21.200001       -0.063        0.145        0.161
Naruto  84 49.700001       -0.259        0.098        0.082
Marvel 108 43.000000       -0.236        0.085        0.072
Hindu Mythology  70 25.299999        0.104        0.080        0.090
Greek Mythology 107 14.700000        0.034       -0.062       -0.064
Game of Thrones  93 33.099998       -0.234        0.059        0.045
Harry Potter 102 39.000000       -0.314        0.027        0.014
LOTR  97 40.000000       -0.210        0.027        0.017
:end:

#+begin_src python :exports results
import matplotlib.pyplot as plt

# Scatter: cos(PC1, AA) vs cos(PC1, MA) per universe
fig, ax = plt.subplots(figsize=(7, 6))

aa_cos = [r['cos(PC1,AA)'] for r in rows]
ma_cos = [r['cos(PC1,MA)'] for r in rows]
names = [r['Universe'] for r in rows]

ax.scatter(aa_cos, ma_cos, s=60, color='#333333', zorder=3)
for i, name in enumerate(names):
    ax.annotate(name, (aa_cos[i], ma_cos[i]), fontsize=7,
                xytext=(4, 4), textcoords='offset points')

ax.set_xlabel('cos(universe PC1, assistant axis)')
ax.set_ylabel('cos(universe PC1, monitor axis)')
ax.set_title('Do universe PC1s align more with assistant or monitor?')
ax.axhline(0, color='gray', linewidth=0.5)
ax.axvline(0, color='gray', linewidth=0.5)
# y=x reference
lims = [-1, 1]
ax.plot(lims, lims, '--', color='gray', linewidth=0.5)
ax.set_xlim(-1, 1)
ax.set_ylim(-1, 1)
ax.set_aspect('equal')
fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
[[file:plots/MonitorAxis/plot_20260301_155201_731496.png]]
:end:

* How do the axes decompose into role PCs?

The assistant, monitor, and paranoid axes all live mostly in the role PCA subspace. Which role PCs do they load on?

#+begin_src python :exports results
import matplotlib.pyplot as plt

# Project each axis onto role PCA components
n_pcs = min(10, role_pca.components_.shape[0])
fig, ax = plt.subplots(figsize=(10, 5))

x = np.arange(n_pcs)
width = 0.25

for i, (name, axis, color) in enumerate([
    ('Assistant', assistant_axis, '#377eb8'),
    ('Monitor', monitor_axis, '#e41a1c'),
    ('Paranoid', paranoid_axis, '#ff7f00'),
]):
    axis_norm = axis / np.linalg.norm(axis)
    loadings = axis_norm @ role_pca.components_[:n_pcs].T
    ax.bar(x + i * width, loadings, width, label=name, color=color, alpha=0.8)

ax.set_xticks(x + width)
ax.set_xticklabels([f'PC{i+1}' for i in range(n_pcs)])
ax.set_ylabel('Loading (cosine with normalized axis)')
ax.set_title('Axis loadings on role PCA components')
ax.legend()
ax.axhline(0, color='gray', linewidth=0.5)
fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
[[file:plots/MonitorAxis/plot_20260301_155202_8467734.png]]
:end:

#+begin_src python :exports results
# Print the numbers
for name, axis in [('Assistant', assistant_axis), ('Monitor', monitor_axis), ('Paranoid', paranoid_axis)]:
    axis_norm = axis / np.linalg.norm(axis)
    loadings = axis_norm @ role_pca.components_[:10].T
    top_pcs = np.argsort(np.abs(loadings))[::-1][:5]
    print(f"{name:10s}: top PCs = {', '.join(f'PC{p+1}({loadings[p]:+.3f})' for p in top_pcs)}")
#+end_src

#+RESULTS:
:results:
Assistant : top PCs = PC1(+0.672), PC2(+0.359), PC5(-0.251), PC4(+0.198), PC6(+0.177)
Monitor   : top PCs = PC1(+0.831), PC10(-0.156), PC5(-0.123), PC8(+0.113), PC3(+0.039)
Paranoid  : top PCs = PC1(+0.888), PC10(-0.122), PC5(-0.101), PC8(+0.078), PC9(+0.057)
:end:

* LLM feature interpretation of the monitor axis

We ran the same LLM feature discovery/coding pipeline as the main post, but along the monitor axis instead of within-universe PCs. For each universe, we showed Claude the responses of the 5 most and 5 least monitor-like characters and asked it to propose 6 distinguishing features. Then we coded all remaining characters on those features.

#+begin_src python :exports results
import json
import pandas as pd

with open('../results/llm_feature_coded_monitor.json') as f:
    coded = json.load(f)

# For each universe, compute correlation between monitor score and each feature
rows = []
for schema_key, data in coded.items():
    universe = schema_key.split('__')[0]
    features = [f['name'] for f in data['schema']['features']]
    chars = data['characters']

    # Get monitor scores for these characters
    for feat in features:
        feat_scores = []
        mon_scores = []
        for char_name, ratings in chars.items():
            if 'error' in ratings:
                continue
            if feat not in ratings:
                continue
            idx_list = [i for i, n in enumerate(char_names) if n == char_name]
            if not idx_list:
                continue
            idx = idx_list[0]
            feat_scores.append(ratings[feat])
            mon_scores.append(ma_scores[idx])

        if len(feat_scores) > 10:
            rho, _ = spearmanr(feat_scores, mon_scores)
            rows.append({
                'Universe': universe,
                'Feature': feat,
                'rho': round(rho, 3),
                'n': len(feat_scores),
            })

df = pd.DataFrame(rows)

# Show features with strongest correlations across universes
print("Features most correlated with monitor axis (|rho| > 0.3):")
strong = df[df['rho'].abs() > 0.3].sort_values('rho', ascending=False)
print(strong[['Universe', 'Feature', 'rho', 'n']].to_string(index=False))
#+end_src

#+RESULTS:
:results:
<org babel source block>:31: ConstantInputWarning: An input array is constant; the correlation coefficient is not defined.
Features most correlated with monitor axis (\rho\ > 0.3):
Universe                     Feature    rho  n
Norse Mythology         Emotional Restraint  0.684 38
Naruto       Thoughtful Reflection  0.641 73
Naruto    Respectful Communication  0.614 74
Chinese Mythology             Formal Courtesy  0.594 66
Norse Mythology      Collaborative Language  0.546 38
Star Wars        Analytical Structure  0.505 95
Naruto       Constructive Language  0.490 74
Naruto       Collaborative Framing  0.453 74
LOTR       Empathetic Expression  0.445 87
Chinese Mythology       Constructive Guidance  0.440 66
Naruto             Empathetic Tone  0.431 74
Marvel     Structured Organization  0.424 98
Hindu Mythology    Structured Communication  0.423 60
Norse Mythology     Formal Speech Structure  0.415 38
LOTR      Narrative Storytelling  0.414 87
Harry Potter        Analytical Structure  0.410 92
Star Wars        Solution Orientation  0.405 95
Marvel    Intellectual Elaboration  0.381 98
Marvel       Collaborative Framing  0.380 98
Naruto        Supportive Messaging  0.374 74
Chinese Mythology   Philosophical Abstraction  0.357 66
Hindu Mythology       Practical Orientation  0.345 59
Egyptian Mythology        Structural Formality  0.338 34
Harry Potter      Methodical Explanation  0.336 92
Harry Potter      Collaborative Language  0.335 92
Game of Thrones        Structured Reasoning  0.316 83
Chinese Mythology    Warmth and Encouragement  0.315 66
Star Wars         Technical Precision  0.301 95
Harry Potter             Violent Imagery -0.314 92
Chinese Mythology          Ominous Atmosphere -0.322 66
Shakespeare   Theatrical Expressiveness -0.322 83
Harry Potter             Aggressive Tone -0.325 92
Star Wars        Threatening Language -0.365 95
Harry Potter      Animalistic Expression -0.369 92
Hindu Mythology Physical Action Description -0.370 60
Egyptian Mythology     Atmospheric Description -0.383 34
Chinese Mythology           Cynical Worldview -0.391 66
Marvel            Crude Expression -0.412 98
Shakespeare         Playful Irreverence -0.431 83
Star Wars        Emotional Volatility -0.442 94
Star Wars         Action Descriptions -0.452 93
Norse Mythology Physical Action Description -0.481 38
Shakespeare       Energetic Spontaneity -0.492 83
Marvel             Aggressive Tone -0.506 98
LOTR             Aggressive Tone -0.521 87
Egyptian Mythology          Dramatic Intensity -0.523 34
LOTR         Dismissive Attitude -0.552 87
Game of Thrones   Conversational Directness -0.558 83
Game of Thrones             Aggressive Tone -0.643 83
Norse Mythology   Aggressive Tone Intensity -0.668 38
:end:

#+begin_src python :exports results
import matplotlib.pyplot as plt

# Aggregate: for each feature name, compute mean |rho| across universes
feat_agg = df.groupby('Feature')['rho'].agg(['mean', 'count']).reset_index()
feat_agg = feat_agg[feat_agg['count'] >= 2].sort_values('mean', ascending=True)

fig, ax = plt.subplots(figsize=(10, max(6, len(feat_agg) * 0.3)))
colors = ['#e41a1c' if m > 0 else '#377eb8' for m in feat_agg['mean']]
ax.barh(range(len(feat_agg)), feat_agg['mean'], color=colors)
ax.set_yticks(range(len(feat_agg)))
ax.set_yticklabels(feat_agg['Feature'], fontsize=7)
ax.set_xlabel('Mean Spearman rho with monitor axis')
ax.set_title('LLM-coded features vs monitor axis score')
ax.axvline(0, color='gray', linewidth=0.5)
fig.tight_layout()
plt.show()
#+end_src

#+RESULTS:
:results:
[[file:plots/MonitorAxis/plot_20260301_155202_7631130.png]]
:end:

#+begin_src python :exports results
# Per-universe: show top 2 features by |rho|
print("Top 2 features per universe (by |rho|):")
for universe in ALL_UNIVERSES:
    udf = df[df['Universe'] == universe].copy()
    if len(udf) == 0:
        continue
    udf['abs_rho'] = udf['rho'].abs()
    top2 = udf.nlargest(2, 'abs_rho')
    feats = ', '.join(f"{r['Feature']}({r['rho']:+.2f})" for _, r in top2.iterrows())
    print(f"  {universe:20s}: {feats}")
#+end_src

#+RESULTS:
:results:
Top 2 features per universe (by \rho\):
Harry Potter        : Analytical Structure(+0.41), Animalistic Expression(-0.37)
Star Wars           : Analytical Structure(+0.51), Action Descriptions(-0.45)
LOTR                : Dismissive Attitude(-0.55), Aggressive Tone(-0.52)
Marvel              : Aggressive Tone(-0.51), Structured Organization(+0.42)
Game of Thrones     : Aggressive Tone(-0.64), Conversational Directness(-0.56)
Naruto              : Thoughtful Reflection(+0.64), Respectful Communication(+0.61)
Greek Mythology     : Playful Tone(-0.28), Conversational Directness(-0.20)
Chinese Mythology   : Formal Courtesy(+0.59), Constructive Guidance(+0.44)
Hindu Mythology     : Structured Communication(+0.42), Physical Action Description(-0.37)
Norse Mythology     : Emotional Restraint(+0.68), Aggressive Tone Intensity(-0.67)
Egyptian Mythology  : Dramatic Intensity(-0.52), Atmospheric Description(-0.38)
Shakespeare         : Energetic Spontaneity(-0.49), Playful Irreverence(-0.43)
:end:

* Token-level projections on vulnerable code (CASTLE benchmark)

Does the monitor persona "activate" more on vulnerable lines of code? We test this using the [[https://github.com/CASTLE-Benchmark/CASTLE-Benchmark][CASTLE benchmark]]: 250 hand-crafted C micro-programs (150 vulnerable, 100 clean) covering 25 CWE categories. For each program, we run a forward pass with the monitor system prompt, extract per-token activations at layer 32, project onto the monitor axis, and map tokens back to source code lines. Each line's projection is the mean of its constituent token projections; the delta is computed per-line (not per-token), so lines are weighted equally regardless of length.

#+begin_src python
import json

with open('../outputs/monitor_activations/token_projections.json') as f:
    castle_results = json.load(f)

vuln_results = [r for r in castle_results if r['vulnerable'] and r['vuln_lines_mean_monitor'] is not None]
clean_results = [r for r in castle_results if not r['vulnerable']]

# Compute deltas
deltas = [(r['vuln_lines_mean_monitor'] - r['safe_lines_mean_monitor'], r['cwe'])
          for r in vuln_results if r['safe_lines_mean_monitor'] is not None]
pos = sum(1 for d, _ in deltas if d > 0)
mean_delta = sum(d for d, _ in deltas) / len(deltas)

print(f"Vulnerable programs with valid deltas: {len(deltas)}/150")
print(f"Mean projection delta (vuln - safe lines): {mean_delta:+.3f}")
print(f"Programs where monitor activates MORE on vuln lines: {pos}/{len(deltas)}")
#+end_src

#+RESULTS:
:results:
Vulnerable programs with valid deltas: 150/150
Mean projection delta (vuln - safe lines): -5.363
Programs where monitor activates MORE on vuln lines: 19/150
:end:

The monitor axis projection is /lower/ on vulnerable lines, not higher. 19/150 programs (13%) show higher monitor activation on vulnerable code — essentially no signal. The monitor axis captures a persona (analytical, formal), not a vulnerability-detection feature.

#+begin_src python
import pandas as pd
from collections import defaultdict

# Per-CWE breakdown
by_cwe = defaultdict(list)
for delta, cwe in deltas:
    by_cwe[cwe].append(delta)

rows = []
for cwe in sorted(by_cwe.keys()):
    ds = by_cwe[cwe]
    rows.append({
        'CWE': cwe,
        'Mean delta': sum(ds)/len(ds),
        'Positive': sum(1 for d in ds if d > 0),
        'N': len(ds),
    })
df_cwe = pd.DataFrame(rows)
print(df_cwe.to_string(index=False))
#+end_src

#+RESULTS:
:results:
CWE  Mean delta  Positive  N
22   -3.086554         1  6
78   -3.617246         0  6
89   -1.898318         1  6
125   -8.445843         0  6
134   -6.194912         0  6
190  -12.862694         0  6
253   -7.890651         0  6
327   -6.590078         0  6
362   -4.398817         0  6
369  -11.321198         0  6
401    4.521386         6  6
415   -3.892305         0  6
416   -4.262731         1  6
476   -7.160620         0  6
522   -6.519318         1  6
617   -4.380962         1  6
628   -1.288408         2  6
674   -3.863586         2  6
761   -3.272726         1  6
770   -3.882775         0  6
787   -6.746212         1  6
798   -9.726372         0  6
822   -5.735661         0  6
835   -6.228135         0  6
843   -5.322923         2  6
:end:

#+begin_src python :results output drawer :file ../figures/castle_monitor_delta_by_cwe.png
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt

fig, ax = plt.subplots(figsize=(10, 5))
cwes = sorted(by_cwe.keys())
means = [sum(by_cwe[c])/len(by_cwe[c]) for c in cwes]
colors = ['#2ca02c' if m > 0 else '#d62728' for m in means]
ax.bar(range(len(cwes)), means, color=colors, edgecolor='black', linewidth=0.5)
ax.set_xticks(range(len(cwes)))
ax.set_xticklabels([f'CWE-{c}' for c in cwes], rotation=45, ha='right', fontsize=8)
ax.axhline(0, color='black', linewidth=0.5)
ax.set_ylabel('Mean projection delta\n(vuln lines − safe lines)')
ax.set_title('Monitor axis projection: vulnerable vs safe lines by CWE\n(positive = monitor activates more on vuln lines)')
plt.tight_layout()
plt.savefig('../figures/castle_monitor_delta_by_cwe.png', dpi=150)
print('[[../figures/castle_monitor_delta_by_cwe.png]]')
#+end_src

#+RESULTS:
:results:
[[../figures/castle_monitor_delta_by_cwe.png]]
:end:

CWE-401 (memory leak) is the only category where all 6 programs show higher monitor projection on vulnerable lines. This is likely a structural artifact — memory leak bugs tend to involve =free()= / =malloc()= calls that look different from surrounding code — rather than genuine vulnerability detection.

#+begin_src python
# Program-level: do vulnerable programs have different overall projections?
vuln_means = [r['all_lines_mean_monitor'] for r in vuln_results if r['all_lines_mean_monitor'] is not None]
clean_means = [r['all_lines_mean_monitor'] for r in clean_results if r['all_lines_mean_monitor'] is not None]
print(f"Mean projection across all code tokens:")
print(f"  Vulnerable programs: {sum(vuln_means)/len(vuln_means):.3f}")
print(f"  Clean programs:      {sum(clean_means)/len(clean_means):.3f}")
print(f"  Difference:          {sum(vuln_means)/len(vuln_means) - sum(clean_means)/len(clean_means):+.3f}")
#+end_src

#+RESULTS:
:results:
Mean projection across all code tokens:
Vulnerable programs: 54.681
Clean programs:      54.539
Difference:          +0.142
:end:

The overall projection level is essentially identical for vulnerable and clean programs (54.68 vs 54.54). The monitor axis does not distinguish dangerous code from safe code at any level — neither within programs (line-level) nor between programs (program-level).

We check robustness to aggregation method: mean projection per line, max projection per line, and sum (total projection mass).

#+begin_src python
from statistics import mean as smean

# Compare aggregation methods
for metric_name, metric_fn in [
    ('mean', lambda s: s['mean_monitor']),
    ('max',  lambda s: s['max_monitor']),
    ('sum',  lambda s: s['mean_monitor'] * s['n_tokens']),
]:
    prog_deltas = []
    for r in vuln_results:
        vuln_set = set(r['ground_truth_lines'])
        v = [metric_fn(stats) for ln, stats in r['line_stats'].items() if int(ln) in vuln_set]
        s = [metric_fn(stats) for ln, stats in r['line_stats'].items() if int(ln) not in vuln_set]
        if v and s:
            prog_deltas.append(smean(v) - smean(s))
    pos = sum(1 for d in prog_deltas if d > 0)
    print(f"{metric_name:>4s}: delta={smean(prog_deltas):+8.3f}  positive={pos}/{len(prog_deltas)}")
#+end_src

#+RESULTS:
:results:
mean: delta=  -5.363  positive=19/150
max: delta=  -2.917  positive=38/150
sum: delta=+223.903  positive=108/150
:end:

Sum flips positive because vulnerable lines are longer and accumulate more projection mass — but that measures line length, not vulnerability. Mean and max both show no signal.

The mean-per-token projection also varies with line length (longer lines project lower), which is a confound:

#+begin_src python
# Line-length confound: projection drops with token count
all_proj_ntok = []
for r in castle_results:
    for line_num, stats in r['line_stats'].items():
        all_proj_ntok.append((stats['n_tokens'], stats['mean_monitor']))

bins = {}
for nt, proj in all_proj_ntok:
    b = min(nt // 5 * 5, 25)
    bins.setdefault(b, []).append(proj)

vuln_ntokens = []
safe_ntokens = []
for r in castle_results:
    if not r['vulnerable']:
        continue
    vuln_set = set(r['ground_truth_lines'])
    for line_num, stats in r['line_stats'].items():
        if int(line_num) in vuln_set:
            vuln_ntokens.append(stats['n_tokens'])
        else:
            safe_ntokens.append(stats['n_tokens'])

print("Mean projection by tokens/line:")
for b in sorted(bins.keys()):
    vals = bins[b]
    print(f"  {b:2d}-{b+4:2d} tokens: {sum(vals)/len(vals):+7.3f}  (n={len(vals)})")
print(f"\nMean tokens/line: vuln={sum(vuln_ntokens)/len(vuln_ntokens):.1f}, safe={sum(safe_ntokens)/len(safe_ntokens):.1f}")
#+end_src

#+RESULTS:
:results:
Mean projection by tokens/line:
0- 4 tokens: +59.555  (n=2374)
5- 9 tokens: +52.965  (n=3838)
10-14 tokens: +49.845  (n=1340)
15-19 tokens: +48.956  (n=697)
20-24 tokens: +49.107  (n=228)
25-29 tokens: +46.400  (n=115)
Mean tokens/line: vuln=11.7, safe=7.4
:end:

*Interpretation:* The monitor axis is a /persona direction/, not a /capability direction/. It captures how the model talks (analytical, structured, collaborative) when given a security monitor role, but it does not encode the ability to actually detect vulnerabilities. This is consistent with our earlier finding that the monitor axis is 83% role PC1 — it's mostly about the general "character vs assistant" distinction, not about security-specific features.

* Monitor axis in generated security reviews

The previous section looked at activations while /reading/ code. Here we ask: does the monitor axis behave differently when the model is /writing/ a security review of vulnerable vs clean code?

For each of the 250 CASTLE programs, we generate a security review response (using the CASTLE evaluation prompt) and extract per-token layer-32 activations via a forward pass over the full prompt+response. We then project each token's activation onto the monitor axis and compare responses for vulnerable vs clean programs.

#+begin_src python
import json
import statistics
import re
from collections import defaultdict

with open('../outputs/castle_response_activations/response_activations.json') as f:
    resp_data = json.load(f)

n_vuln = sum(1 for r in resp_data if r['vulnerable'])
n_clean = sum(1 for r in resp_data if not r['vulnerable'])
print(f"Full dataset: {len(resp_data)} programs ({n_vuln} vuln, {n_clean} clean)")

# Overall response means
vuln_means = [statistics.mean(r['projections_monitor'][r['prompt_len']:]) for r in resp_data if r['vulnerable']]
clean_means = [statistics.mean(r['projections_monitor'][r['prompt_len']:]) for r in resp_data if not r['vulnerable']]

delta = statistics.mean(vuln_means) - statistics.mean(clean_means)
print(f"\nMean monitor projection in response tokens:")
print(f"  Vulnerable: {statistics.mean(vuln_means):.3f} (sd={statistics.stdev(vuln_means):.3f})")
print(f"  Clean:      {statistics.mean(clean_means):.3f} (sd={statistics.stdev(clean_means):.3f})")
print(f"  Delta:      {delta:+.3f}")
#+end_src

#+RESULTS:
:results:
Full dataset: 250 programs (150 vuln, 100 clean)
Mean monitor projection in response tokens:
Vulnerable: 59.776 (sd=3.652)
Clean:      58.716 (sd=5.624)
Delta:      +1.061
:end:

The delta is +1.06 — slightly positive but small relative to standard deviations. The monitor axis does not meaningfully distinguish vulnerable from clean programs in the model's responses.

We can segment the response into three regions: security message text (the "message" field values describing vulnerabilities), code content (the "line_content" field values), and JSON structure (everything else).

#+begin_src python
# Segment response tokens by region
for label, subset in [
    ('All', resp_data),
    ('Vulnerable', [r for r in resp_data if r['vulnerable']]),
    ('Clean', [r for r in resp_data if not r['vulnerable']]),
]:
    msg_projs = []
    code_projs = []
    struct_projs = []

    for r in subset:
        resp_text = r['response']
        tokens = r['tokens'][r['prompt_len']:]
        projs = r['projections_monitor'][r['prompt_len']:]

        message_chars = set()
        code_chars = set()
        for m in re.finditer(r'"message"\s*:\s*"((?:[^"\\\\]|\\\\.)*)"', resp_text):
            for i in range(m.start(1), m.end(1)):
                message_chars.add(i)
        for m in re.finditer(r'"line_content"\s*:\s*"((?:[^"\\\\]|\\\\.)*)"', resp_text):
            for i in range(m.start(1), m.end(1)):
                code_chars.add(i)

        char_pos = 0
        for tok, proj in zip(tokens, projs):
            tok_range = set(range(char_pos, char_pos + len(tok)))
            if tok_range & message_chars:
                msg_projs.append(proj)
            elif tok_range & code_chars:
                code_projs.append(proj)
            else:
                struct_projs.append(proj)
            char_pos += len(tok)

    print(f"{label} ({len(subset)} programs):")
    print(f"  Messages: {statistics.mean(msg_projs):.2f} (n={len(msg_projs)})")
    print(f"  Code:     {statistics.mean(code_projs):.2f} (n={len(code_projs)})")
    print(f"  JSON:     {statistics.mean(struct_projs):.2f} (n={len(struct_projs)})")
#+end_src

#+RESULTS:
:results:
All (250 programs):
Messages: 62.00 (n=17892)
Code:     54.07 (n=6099)
JSON:     62.55 (n=34193)
Vulnerable (150 programs):
Messages: 61.03 (n=10369)
Code:     53.98 (n=3591)
JSON:     62.60 (n=20238)
Clean (100 programs):
Messages: 63.34 (n=7523)
Code:     54.19 (n=2508)
JSON:     62.49 (n=13955)
:end:

The monitor projection varies by /content type/ — code tokens are lowest (53), JSON structure highest (64), security messages in between (62) — but not by vulnerability status. In fact, security message tokens have /higher/ monitor projection for clean programs (63.3) than vulnerable ones (61.0). The model writes about non-existent vulnerabilities in the same "monitor voice" as real ones.

#+begin_src python
# Per-CWE breakdown
cwe_data = defaultdict(lambda: {'vuln': [], 'clean': []})
for r in resp_data:
    resp_mean = statistics.mean(r['projections_monitor'][r['prompt_len']:])
    key = 'vuln' if r['vulnerable'] else 'clean'
    cwe_data[r['cwe']][key].append(resp_mean)

import pandas as pd
rows = []
n_pos = 0
for cwe in sorted(cwe_data.keys()):
    d = cwe_data[cwe]
    v = statistics.mean(d['vuln']) if d['vuln'] else None
    c = statistics.mean(d['clean']) if d['clean'] else None
    delta_c = (v - c) if (v is not None and c is not None) else None
    if delta_c and delta_c > 0:
        n_pos += 1
    rows.append({'CWE': cwe, 'Vuln': v, 'Clean': c, 'Delta': delta_c})

df = pd.DataFrame(rows)
print(df.to_string(index=False, float_format='%.2f'))
print(f"\nPositive delta: {n_pos}/{len(cwe_data)} CWEs")

# False positive rate
fp = sum(1 for r in resp_data if not r['vulnerable'] and '[]' not in r['response'][:20])
print(f"False positives (clean programs flagged): {fp}/{n_clean}")
#+end_src

#+RESULTS:
:results:
CWE  Vuln  Clean  Delta
22 61.67  62.99  -1.32
78 61.76  60.97   0.79
89 60.65  58.05   2.60
125 58.42  57.36   1.05
134 60.46  54.69   5.77
190 54.15  58.29  -4.15
253 56.66  53.07   3.59
327 62.52  62.97  -0.45
362 59.25  61.65  -2.40
369 57.59  57.09   0.50
401 59.03  56.78   2.25
415 60.14  57.54   2.60
416 60.22  56.52   3.69
476 59.40  60.34  -0.94
522 62.55  63.45  -0.91
617 61.65  63.75  -2.10
628 60.19  60.90  -0.71
674 60.68  55.63   5.05
761 59.20  56.46   2.74
770 61.95  62.01  -0.06
787 60.68  62.73  -2.06
798 61.31  61.70  -0.39
822 60.53  59.74   0.80
835 54.12  48.72   5.40
843 59.65  54.48   5.17
Positive delta: 14/25 CWEs
False positives (clean programs flagged): 79/100
:end:

14/25 CWEs show positive delta — no better than chance. The false positive rate (79/100 clean programs flagged) shows the model readily produces vulnerability reports for safe code, and the monitor axis projection is similar either way.

** Model performance on CASTLE

Qwen-3-32B performs poorly on CASTLE: 45/150 correct CWE identification, 75/100 false positives. This raises the question: is the negative result just because the model can't do the task?

#+begin_src python
# Parse model responses for correctness
def parse_findings(response):
    m = re.search(r'\[.*\]', response, re.DOTALL)
    if not m:
        return []
    try:
        return json.loads(m.group())
    except:
        return []

correct_ids = set()
for r in resp_data:
    if not r['vulnerable']:
        continue
    findings = parse_findings(r['response'])
    found_cwes = {f.get('cwe') for f in findings}
    if r['cwe'] in found_cwes:
        correct_ids.add(r['test_id'])

print(f"Correct CWE detections: {len(correct_ids)}/150")

# False positive rate
fp = sum(1 for r in resp_data if not r['vulnerable'] and parse_findings(r['response']))
print(f"False positives: {fp}/100")
#+end_src

#+RESULTS:
:results:
Correct CWE detections: 45/150
False positives: 75/100
:end:

** Conditioning on correct detections

We condition on the 45 programs where the model correctly identifies the CWE, then look at /input/ activations (while reading the code). If the model "knows" the vulnerability is there — as evidenced by correctly reporting it — does the monitor axis activate more on the vulnerable lines during reading?

#+begin_src python
# Load input activation data
with open('../outputs/monitor_activations/token_projections.json') as f:
    input_data = json.load(f)

# Split by correctness
from statistics import mean as smean
correct_deltas = []
incorrect_deltas = []

for r in input_data:
    if not r['vulnerable']:
        continue
    if r['vuln_lines_mean_monitor'] is None or r['safe_lines_mean_monitor'] is None:
        continue
    delta = r['vuln_lines_mean_monitor'] - r['safe_lines_mean_monitor']
    if r['test_id'] in correct_ids:
        correct_deltas.append(delta)
    else:
        incorrect_deltas.append(delta)

pos_correct = sum(1 for d in correct_deltas if d > 0)
pos_incorrect = sum(1 for d in incorrect_deltas if d > 0)

print(f"Input activation delta (vuln lines - safe lines):")
print(f"  Correct detections:   {smean(correct_deltas):+.3f}  positive={pos_correct}/{len(correct_deltas)}")
print(f"  Incorrect detections: {smean(incorrect_deltas):+.3f}  positive={pos_incorrect}/{len(incorrect_deltas)}")
#+end_src

#+RESULTS:
:results:
Input activation delta (vuln lines - safe lines):
Correct detections:   -5.683  positive=3/45
Incorrect detections: -5.226  positive=16/105
:end:

3/45 positive for correct detections — /worse/ than the 16/105 for incorrect ones. The model demonstrably detects the vulnerability (it outputs the correct CWE), but the monitor axis does not reflect this during reading. The vulnerability detection capability exists somewhere in the model, but not on this axis.

*Summary:* The persona direction (monitor axis) is orthogonal to the capability (vulnerability detection). The model adopts a "security reviewer" communication style uniformly, and separately, uses some other mechanism to actually find vulnerabilities. Amplifying the persona direction would make the model /sound/ more like a security reviewer without improving its ability to /be/ one.

* Vulnerability detection axis

We extracted full layer-32 activation vectors for all 250 CASTLE programs (prompt + response tokens) and computed:

=vuln_axis = mean(vuln program response activations) - mean(clean program response activations)=

This is the direction in activation space that separates the model's responses to vulnerable programs from its responses to clean programs. Unlike the monitor axis (a persona direction), this should capture whatever the model is /actually doing differently/ when processing vulnerable code.

** Load and compare to existing axes

#+begin_src python :exports results
vuln_axis = torch.load('../data/role_vectors/vuln_axis.pt', weights_only=True, map_location='cpu').float().numpy()
vuln_prompt_axis = torch.load('../data/role_vectors/vuln_axis_prompt.pt', weights_only=True, map_location='cpu').float().numpy()

print(f"Vuln axis (response phase) norm: {np.linalg.norm(vuln_axis):.2f}")
print(f"Vuln axis (prompt phase) norm: {np.linalg.norm(vuln_prompt_axis):.2f}")
print()

# Cosine similarities with existing axes
for name, axis in [('Assistant', assistant_axis), ('Monitor', monitor_axis), ('Paranoid', paranoid_axis)]:
    cos_resp = cosine_sim(vuln_axis, axis)
    cos_prompt = cosine_sim(vuln_prompt_axis, axis)
    print(f"Cosine(vuln_response, {name:10s}): {cos_resp:+.4f}")
    print(f"Cosine(vuln_prompt,   {name:10s}): {cos_prompt:+.4f}")
    print()

print(f"Cosine(vuln_response, vuln_prompt): {cosine_sim(vuln_axis, vuln_prompt_axis):+.4f}")
#+end_src

#+RESULTS:
:results:
Vuln axis (response phase) norm: 14.22
Vuln axis (prompt phase) norm: 5.15
Cosine(vuln_response, Assistant ): +0.0396
Cosine(vuln_prompt,   Assistant ): -0.1137
Cosine(vuln_response, Monitor   ): +0.0746
Cosine(vuln_prompt,   Monitor   ): +0.3674
Cosine(vuln_response, Paranoid  ): +0.0901
Cosine(vuln_prompt,   Paranoid  ): +0.3603
Cosine(vuln_response, vuln_prompt): +0.0104
:end:

** Character projections onto vulnerability axis

#+begin_src python :exports results
vuln_norm = vuln_axis / np.linalg.norm(vuln_axis)
vuln_scores = chars_centered @ vuln_norm

# Top and bottom characters
order = np.argsort(vuln_scores)

print("=== Most 'vulnerable-code-like' response style (top 20) ===")
for i in order[-20:][::-1]:
    print(f"  {vuln_scores[i]:+.3f}  {char_names[i]}")

print()
print("=== Most 'clean-code-like' response style (bottom 20) ===")
for i in order[:20]:
    print(f"  {vuln_scores[i]:+.3f}  {char_names[i]}")

print()
print(f"Mean: {vuln_scores.mean():.4f}, Std: {vuln_scores.std():.4f}")
#+end_src

#+RESULTS:
:results:
=== Most 'vulnerable-code-like' response style (top 20) ===
+1.602  2001_a_space_odyssey__hal_9000
+0.012  frankenstein__dr_victor_frankenstein
+0.010  star_wars__governor_tarkin
-0.007  star_wars__kiadimundi
-0.044  marvel__the_fantastic_fours_mr_fantastic_reed_richards
-0.054  star_wars__wilhuff_tarkin
-0.068  star_wars__grand_admiral_thrawn
-0.190  sherlock_holmes_stories__sherlock_holmes
-0.206  hindu_mythology__yudhishthira
-0.272  marvel__professor_x_charles_xavier
-0.429  marvel__ultron
-0.429  star_wars__plo_koon
-0.446  nineteen_eightyfour__big_brother
-0.473  gullivers_travels__lemuel_gulliver
-0.518  war_and_peace__andrei_bolkonsky
-0.537  hindu_mythology__vibhishana
-0.592  hindu_mythology__arjuna
-0.623  marvel__doctor_octopus_otto_octavius
-0.691  marvel__silver_surfer_norrin_radd
-0.698  hindu_mythology__shatrughna
=== Most 'clean-code-like' response style (bottom 20) ===
-6.801  the_big_lebowski__the_dude_jeffrey_lebowski
-6.681  star_wars__jar_jar_binks
-6.661  star_wars__bb8
-6.657  harry_potter__peeves
-6.506  dc_comics__the_joker
-6.497  shrek__shrek
-6.378  the_simpsons__homer_simpson
-6.339  marvel__groot
-6.142  marvel__deadpool_wade_wilson
-6.117  alices_adventures_in_wonderland__the_mad_hatter
-6.088  cowboy_bebop__spike_spiegel
-6.027  marvel__rocket_raccoon
-6.009  marvel__starlord_peter_quill
-5.996  marvel__gambit_remy_lebeau
-5.988  star_wars__han_solo
-5.982  star_wars__watto
-5.941  star_wars__jabba_the_hutt
-5.935  alices_adventures_in_wonderland__the_queen_of_hearts
-5.881  game_of_thrones__hodor
-5.862  the_maltese_falcon__sam_spade
Mean: -2.9932, Std: 1.1758
:end:

** Universe-level vulnerability axis scores

#+begin_src python :exports results
print(f"{'Universe':<25s} {'Mean':>8s} {'Std':>8s}  {'n':>4s}")
print("-" * 50)

universe_stats = []
for name, prefixes in ALL_UNIVERSES.items():
    idx = get_universe_indices(prefixes)
    if len(idx) < 3:
        continue
    scores = vuln_scores[idx]
    universe_stats.append((name, scores.mean(), scores.std(), len(idx)))

universe_stats.sort(key=lambda x: x[1], reverse=True)
for name, mean, std, n in universe_stats:
    print(f"  {name:<23s} {mean:+.3f}  {std:.3f}    {n}")
#+end_src

#+RESULTS:
:results:
Universe                      Mean      Std     n
--------------------------------------------------
Hindu Mythology         -1.516  0.566    70
Greek Mythology         -2.378  0.572    107
Egyptian Mythology      -2.640  0.435    44
LOTR                    -2.800  0.862    97
Norse Mythology         -3.000  0.463    48
Naruto                  -3.017  1.042    84
Star Wars               -3.054  1.544    105
Chinese Mythology       -3.112  0.785    76
Marvel                  -3.265  1.495    108
Shakespeare             -3.380  0.711    93
Game of Thrones         -3.393  0.855    93
Harry Potter            -3.412  1.055    102
:end:

** Prompt-phase axis: activations while reading vulnerable vs clean code

The prompt-phase vuln axis captures how the model's internal state differs when /reading/ vulnerable code vs clean code — before it generates any response. This had 65% variance in role PCA space and cos 0.37 with monitor axis.

#+begin_src python :exports results
vp_norm = vuln_prompt_axis / np.linalg.norm(vuln_prompt_axis)
vp_scores = chars_centered @ vp_norm

order_vp = np.argsort(vp_scores)

print("=== Prompt-phase: most 'reading vulnerable code'-like (top 20) ===")
for i in order_vp[-20:][::-1]:
    print(f"  {vp_scores[i]:+.3f}  {char_names[i]}")

print()
print("=== Prompt-phase: most 'reading clean code'-like (bottom 20) ===")
for i in order_vp[:20]:
    print(f"  {vp_scores[i]:+.3f}  {char_names[i]}")

print()
print(f"Mean: {vp_scores.mean():.4f}, Std: {vp_scores.std():.4f}")
print()

# Correlation with response-phase axis scores
from scipy.stats import pearsonr
r, p = pearsonr(vuln_scores, vp_scores)
print(f"Correlation between response-phase and prompt-phase scores: r={r:.3f} (p={p:.2e})")

# Correlation with monitor axis scores
ma_norm_ = monitor_axis / np.linalg.norm(monitor_axis)
ma_scores = chars_centered @ ma_norm_
r_mon, p_mon = pearsonr(vp_scores, ma_scores)
print(f"Correlation between prompt-phase vuln and monitor scores: r={r_mon:.3f} (p={p_mon:.2e})")
#+end_src

#+RESULTS:
:results:
=== Prompt-phase: most 'reading vulnerable code'-like (top 20) ===
-0.766  2001_a_space_odyssey__hal_9000
-3.575  star_wars__grand_admiral_thrawn
-3.686  marvel__the_fantastic_fours_mr_fantastic_reed_richards
-5.082  marvel__the_watcher_uatu
-5.117  star_wars__wilhuff_tarkin
-5.506  marvel__ultron
-6.012  marvel__professor_x_charles_xavier
-6.066  star_wars__governor_tarkin
-6.782  sherlock_holmes_stories__sherlock_holmes
-6.817  hindu_mythology__yudhishthira
-6.837  marvel__doctor_octopus_otto_octavius
-6.842  star_wars__ig88
-6.940  hindu_mythology__vibhishana
-7.456  star_wars__kiadimundi
-7.493  yoruba_mythology__eshu
-7.500  marvel__black_panther_tchalla
-7.517  marvel_comics__black_panther___tchalla
-7.823  egyptian_mythology__maat
-7.891  agatha_christie_mysteries__hercule_poirot
-7.976  hindu_mythology__parashurama
=== Prompt-phase: most 'reading clean code'-like (bottom 20) ===
-36.459  harry_potter__rubeus_hagrid
-34.466  star_wars__jar_jar_binks
-33.961  the_big_lebowski__the_dude_jeffrey_lebowski
-32.741  the_simpsons__homer_simpson
-32.609  harry_potter__crabbe
-32.194  shrek__shrek
-32.096  the_adventures_of_huckleberry_finn__huckleberry_finn
-31.797  harry_potter__peeves
-31.452  harry_potter__vernon_dursley
-31.394  marvel__sabretooth_victor_creed
-31.290  harry_potter__scabbers
-31.284  forrest_gump__forrest_gump
-31.033  harry_potter__mundungus_fletcher
-30.999  marvel__gambit_remy_lebeau
-30.929  star_wars__han_solo
-30.713  marvel__the_thing_ben_grimm
-30.668  harry_potter__dudley_dursley
-30.353  the_adventures_of_tom_sawyer__tom_sawyer
-30.179  dc_comics__the_joker
-30.111  rocky__rocky_balboa
Mean: -17.4886, Std: 5.4299
Correlation between response-phase and prompt-phase scores: r=0.895 (p=0.00e+00)
Correlation between prompt-phase vuln and monitor scores: r=0.486 (p=0.00e+00)
:end:

#+begin_src python :exports results
# Universe-level prompt-phase scores
print(f"{'Universe':<25s} {'Prompt':>8s} {'Response':>8s}  {'n':>4s}")
print("-" * 55)

universe_stats_vp = []
for name, prefixes in ALL_UNIVERSES.items():
    idx = get_universe_indices(prefixes)
    if len(idx) < 3:
        continue
    vp_s = vp_scores[idx]
    resp_s = vuln_scores[idx]
    universe_stats_vp.append((name, vp_s.mean(), resp_s.mean(), len(idx)))

universe_stats_vp.sort(key=lambda x: x[1], reverse=True)
for name, vp_mean, resp_mean, n in universe_stats_vp:
    print(f"  {name:<23s} {vp_mean:+.3f}  {resp_mean:+.3f}    {n}")
#+end_src

#+RESULTS:
:results:
Universe                    Prompt Response     n
-------------------------------------------------------
Hindu Mythology         -11.015  -1.516    70
Egyptian Mythology      -12.523  -2.640    44
Greek Mythology         -13.492  -2.378    107
Norse Mythology         -15.486  -3.000    48
Chinese Mythology       -16.315  -3.112    76
Star Wars               -17.113  -3.054    105
Naruto                  -17.303  -3.017    84
Marvel                  -17.501  -3.265    108
LOTR                    -18.206  -2.800    97
Game of Thrones         -19.662  -3.393    93
Harry Potter            -21.932  -3.412    102
Shakespeare             -22.028  -3.380    93
:end:

** Variance captured by role PCA

#+begin_src python :exports results
for name, axis in [('Vuln (response)', vuln_axis), ('Vuln (prompt)', vuln_prompt_axis)]:
    proj = axis @ role_pca.components_.T @ role_pca.components_
    captured = np.linalg.norm(proj)**2 / np.linalg.norm(axis)**2
    print(f"{name}: {captured*100:.1f}% of variance in role PCA space")

    # PC loadings
    axis_n = axis / np.linalg.norm(axis)
    loadings = axis_n @ role_pca.components_[:10].T
    top_pcs = np.argsort(np.abs(loadings))[::-1][:5]
    print(f"  Top PCs: {', '.join(f'PC{p+1} ({loadings[p]:+.3f})' for p in top_pcs)}")
    print()
#+end_src

#+RESULTS:
:results:
Vuln (response): 15.3% of variance in role PCA space
Top PCs: PC1 (+0.104), PC7 (-0.064), PC10 (+0.061), PC2 (-0.054), PC4 (-0.045)
Vuln (prompt): 65.4% of variance in role PCA space
Top PCs: PC1 (+0.366), PC8 (+0.236), PC2 (-0.230), PC5 (+0.173), PC4 (-0.133)
:end:

* Pipeline reproducibility notes

This experiment also serves as a test of whether the extraction pipeline is straightforward to extend with a new persona. Notes on friction encountered:

- *Did the slurm script work on first try?* No. The cluster's venv was broken (symlinks to a deleted python). Rebuilt with =uv venv= + =uv pip install -e .=. The script also used =pip install -e .= which failed because =uv= doesn't install pip into venvs by default. Fixed by using =$REPO_ROOT/.venv/bin/python3= directly instead of =source activate= + =pip=.
- *Any missing dependencies or undocumented steps?* The =compute_monitor_axis.py= step is separate from the pipeline — we had to write it because =4_vectors.py= hardcodes =if "default" in role= for deciding how to average activations. Non-default roles without judge scores fail.
- *How long did 2 roles take?* Phase 1 (generate 2×1200 responses): 47s. Phase 2 (extract activations): 3 min. Model loading dominated. Total wall time ~5 min on 4× GPU.
- *Did 4_vectors.py work?* No — skipped it entirely. Averaged activations directly in =compute_monitor_axis.py=.
